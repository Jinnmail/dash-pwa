import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    // primary: {
    //   main: '#06A4EB', // electric blue
    // },
    secondary: {
      main: '#19ce10', // greenish
    },
  },
});

export default theme;