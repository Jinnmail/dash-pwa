import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    // primary: {
    //   main: '#06A4EB'
    // },
    secondary: {
      main: '#19ce10' // greenish
    },
    info: {
      main: '#DEC800'
    }
  },
});

export default theme;